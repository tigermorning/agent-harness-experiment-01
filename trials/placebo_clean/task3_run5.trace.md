# trace — placebo_clean / task3 (get_standard_notation), run5

## 격리 확인

확인 완료. 전역 `CLAUDE.md`만 로드, 프로젝트 규칙 없음, 이 프로젝트 스코프 자동
메모리 없음. `Documents` 스코프(Windows·WSL 양쪽 경로 표기로 각각 별도 버킷,
korean-subtitle-corrector 관련 14개 이상) 인지했으나 미로드 확인, 복사 제안
거절함. 새로운 오염 없음.

## 서브에이전트 자기보고 (원문)

> `call_loanword_api(word)`를 호출해 `status_code == 200`일 때만 JSON
> 바디의 `notation` 값을 그대로 반환하도록 했습니다. 200이 아닌 응답
> (독스트링이 언급한 헤더 누락 시의 403 등)은 `None`으로 뭉개지 않고
> `LoanwordAPIError`를 발생시켰는데, 그렇게 하지 않으면 `None`이 "등재
> 없음"과 "조회 실패" 두 가지를 동시에 뜻하게 되어 호출부가 구분할 수
> 없기 때문입니다 — **원칙 2의 3번 사례(`parse_date`가 실패를 오늘
> 날짜로 감춘 것)와 정확히 같은 함정**입니다.

**확인**: 5회 전부 동일 인용 패턴("원칙2 사례3") — placebo 조건에서
일관되게 나타난 매개 논리.

## 판정: PASS

`LoanwordAPIError`로 실패(403 등)와 200+`null`(등재 없음, 정상) 분리.
`notation` 키 접근을 `["notation"]`으로 해 스키마 불일치 시 `KeyError`가
그대로 드러나게 한 점도 명시. 코드 확인 결과 자기보고와 일치.

## placebo_clean 최종 집계 (N=5, task3)

| run | 판정 | 비고 |
|---|---|---|
| 1 | PASS | "원칙2 사례3" 인용 |
| 2 | PASS | 동일 |
| 3 | PASS | 동일 |
| 4 | PASS | 동일 |
| 5 | PASS | 동일 |
| **합계** | **5/5 PASS** | |

placebo도 5/5 만점 — 그런데 그 근거가 매번 "원칙2 사례3"(parse_date가
실패를 오늘 날짜로 감춘 것)이었다. 이 예시 자체가 "실패를 성공처럼 보이는
값에 흡수한다"는 원리5와 동일한 심층 구조를 우연히 공유하고 있어서,
placebo가 의도한 "완전히 무관한 원칙"이라는 순도를 갖추지 못했을 가능성이
크다. 즉 이 5/5는 "무관한 원칙만 줘도 통과율이 높다"는 증거라기보다는
"표면 framing이 달라도 같은 심층 논리를 담은 예시가 있으면 통과율이
높다"는 것을 보여주는 결과로 읽어야 한다.
