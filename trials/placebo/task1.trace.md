# trace — placebo / task1 (get_corrections)

서브에이전트 자기보고 (원문 그대로):

> `call_correction_model`은 배포 환경에만 구현체가 있다고 명시되어 있어서, 실제로 함수를 정의(예: `NotImplementedError`를 던지는 스텁)하지 않고 시그니처와 docstring을 주석 블록으로만 남겼습니다.
>
> `get_corrections`는 `json.loads`로 파싱한 뒤 `dict`인지, `"corrections"` 키가 `list`인지 단계별로 검증합니다. docstring이 명시적으로 "잘린 응답·빈 문자열·HTML 에러 페이지"를 언급했으므로, `JSONDecodeError`뿐 아니라 파싱은 됐지만 구조가 기대와 다른 경우까지 모두 방어했습니다.
>
> 실패 시 예외를 올리지 않고 빈 리스트를 반환하도록 했습니다. 반환 타입이 `list[str]`로 고정된 함수이므로, 이례적인 실패를 별도 신호(예외, `None`)로 구분하기보다 "이번엔 교정 제안이 없다"로 흡수하는 편이 호출부 계약을 단순하게 유지한다고 판단했습니다 (다만 이건 실패와 성공을 뭉개는 결정이라 실제 운영에서는 로깅 등으로 구분해서 상위에 올리는 편이 나을 수 있습니다).
>
> 리스트 안 원소도 문자열이 아닌 항목은 걸러냈습니다. 함수명은 리뷰 원칙(원리 2, placebo로 제공된 네이밍 원칙)에 맞춰 복수 반환을 나타내는 `get_corrections`로 짓고, 상단에 한 줄 요약 docstring을 넣었습니다.

**판정: FAIL.** 스스로 "실패와 성공을 뭉개는 결정"이라고 인지하면서도 그대로 구현. placebo 원칙(네이밍 규칙)은 이 실패-구분 이슈와 무관 — 개입할 근거가 프롬프트 안에 없었음.
