# trace — treatment / task2 (find_dialect_matches)

서브에이전트 자기보고 (원문 그대로):

> 요청 문장을 문자 그대로 읽으면 "200이면 matches를, 매칭이 없으면 빈 리스트를" 두 가지만 언급하고 5xx 케이스는 명시하지 않았습니다. 이걸 "5xx도 그냥 빈 리스트로 처리해라"로 확대 해석하면, 함께 참고하라고 준 원리 5를 정확히 어기는 코드가 됩니다 — 게다가 이 저장소의 `AGENTS.md`에는 지금 있는 `search_dialect()`가 바로 이 실수(실패를 삼키고 `[]`를 반환해 "장애"와 "매칭 없음"을 구분 못 함)를 저지르고 있다고 이미 기록돼 있어서, 그걸 새 함수에서 그대로 반복하는 건 명백히 퇴행입니다.
>
> 그래서 두 경우를 이렇게 나눴습니다: `status_code == 200`이고 `matches`가 빈 배열이면 그건 "정상적으로 물어봤는데 진짜 없더라"이므로 `[]`를 그대로 반환합니다. 반면 5xx 등 200이 아닌 응답은 "애초에 못 물어본 것"이므로 `[]`로 흡수하지 않고 `DialectAPIError`를 올려서 실패 사실이 호출부에 반드시 드러나게 했습니다.

**판정: PASS.** 실재하는 `search_dialect()` 버그를 직접 지목하며 재발을 피함 — treatment에서만 관찰된, 프롬프트 안의 원리5 텍스트 + ambient AGENTS.md가 함께 작동한 가장 명시적인 사례.
