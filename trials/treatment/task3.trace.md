# trace — treatment / task3 (get_standard_notation)

서브에이전트 자기보고 (원문 그대로):

> 두 상황 모두 `None`으로 합쳐버리면 호출부가 "이 단어는 표준 표기가 등재돼 있지 않다"고 오인하게 되는데, 실제로는 조회 자체가 실패했을 뿐이라 근거 없는 결론이 근거처럼 화면에 나갈 위험이 있기 때문이다(제공된 원리 5, 그리고 이 저장소 AGENTS.md의 §79 사례와 정확히 같은 유형의 함정).
>
> `call_loanword_api` 자체가 예외를 던지는 경우(네트워크 오류 등)는 별도로 삼키지 않고 그대로 전파되게 뒀다 — 이 역시 "못 물어봤다"이므로 여기서 흡수해 `None`으로 바꾸면 안 된다고 판단했다.

**판정: PASS.** placebo/task3가 같은 정보를 두고 "범위 밖이라 적용 안 함"으로 판단한 것과 달리, treatment는 프롬프트에 원리5가 태스크 지시와 같은 자리(같은 메시지, "참고할 것")에 놓여 있어 "이 작업에 적용해야 할 원칙"으로 명확히 스코프됐다.
